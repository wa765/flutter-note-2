import 'package:flutter/material.dart';

class MainPageMediaQueryTutorial extends StatelessWidget {
  const MainPageMediaQueryTutorial({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Latihan Media Query"),
      ),
    );
  }
}
